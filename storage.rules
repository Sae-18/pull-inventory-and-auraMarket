rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow read/write when using emulator or in dev mode
      allow read, write: if request.auth != null || request.auth == null;
    }
  }
}
